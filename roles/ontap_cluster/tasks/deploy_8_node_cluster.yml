# roles/ontap_cluster/tasks/deploy_1_node_cluster.yml

---
- name: Deploy 8-node ONTAP cluster
  hosts: cluster_nodes
  tasks:
    - name: Deploying 8-node cluster
      debug:
        msg: "Deploying 8-node ONTAP cluster on {{ inventory_hostname }}"
